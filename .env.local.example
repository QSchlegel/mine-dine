# Database - Use this for local Docker setup
# Note: Using port 5436 to avoid conflicts with local PostgreSQL
# If this port is also in use, change it in docker-compose.yml and update here
DATABASE_URL="postgresql://minedine:minedine@localhost:5436/minedine?schema=public"

# Stripe Payments
# Get your keys from https://dashboard.stripe.com/apikeys (use test keys)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_..."
STRIPE_SECRET_KEY="sk_test_..."
# Get webhook secret from Stripe dashboard webhook settings
STRIPE_WEBHOOK_SECRET="whsec_..."

# App Configuration
# Secret for session management (generate a random string)
NEXTAUTH_SECRET="your-secret-key-here-change-in-production"
# Your app URL
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Umami Analytics
# Get your website ID from your Umami dashboard
# Railway: Set NEXT_PUBLIC_UMAMI_URL to your Umami instance URL (e.g., https://umami.example.com)
# Local: Use your self-hosted Umami URL or leave empty to disable
NEXT_PUBLIC_UMAMI_WEBSITE_ID="your_website_id"
NEXT_PUBLIC_UMAMI_URL="https://umami.example.com"

# File Upload - Storage Configuration
# When PINATA_JWT is set, IPFS (Pinata) is used for all uploads (profile, dinner, dish images, etc.).
# Otherwise S3/Railway is used.

# IPFS (Pinata) - preferred when set:
# Create JWT at https://pinata.cloud/app/keys
PINATA_JWT="eyJhbGciOi..."
# Optional: custom gateway base for URLs (default: https://gateway.pinata.cloud)
PINATA_GATEWAY_URL="https://gateway.pinata.cloud"

# Railway Native Bucket (used when PINATA_JWT is not set):
# When connecting Railway bucket to your service, these are automatically set:
# AWS_ENDPOINT_URL, AWS_S3_BUCKET_NAME, AWS_DEFAULT_REGION, AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY

# External AWS S3 (used when PINATA_JWT is not set):
AWS_S3_BUCKET_NAME="your_bucket_name"
AWS_S3_REGION="us-east-1"  # or AWS_DEFAULT_REGION
AWS_ACCESS_KEY_ID="your_access_key"
AWS_SECRET_ACCESS_KEY="your_secret_key"
# AWS_S3_ENDPOINT="https://s3.us-east-1.amazonaws.com"  # Optional

# AI - Recipe and dinner planning (use one or both)
# OpenAI: https://platform.openai.com/api-keys
OPENAI_API_KEY="sk-..."
# Google Gemini (optional): https://aistudio.google.com/apikey — when set, used for recipe/dinner planning
GEMINI_API_KEY=""

# Dish image (plated recipe) uses OPENAI_API_KEY + DALL-E; generated image is stored via IPFS (if PINATA_JWT set) or S3/Railway

# Admin bootstrap
FIRST_ADMIN_EMAIL="admin@minedine.com"
